import { EventnotificationPipe } from './eventnotification.pipe';

describe('EventnotificationPipe', () => {
  it('create an instance', () => {
    const pipe = new EventnotificationPipe();
    expect(pipe).toBeTruthy();
  });
});
