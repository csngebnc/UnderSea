import { HasbuildingPipe } from './hasbuilding.pipe';

describe('HasbuildingPipe', () => {
  it('create an instance', () => {
    const pipe = new HasbuildingPipe();
    expect(pipe).toBeTruthy();
  });
});
